<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ye.droid.jarvis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activity.HomeActivity"><!--将主界面设置为HOME，去掉Splash页面-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.SettingActivity" />
        <activity android:name=".activity.BurglarsResultActivity" />
        <activity android:name=".activity.BurglarsSetup1Activity" />
        <activity android:name=".activity.BurglarsSetup2Activity" />
        <activity android:name=".activity.BurglarsSetup3Activity" />
        <activity android:name=".activity.ContactListActivity" />
        <activity android:name=".activity.BurglarsSetup4Activity" />
        <activity android:name=".activity.AboutActivity" /> <!--关于软件界面-->
        <!--配置缓存清理页面-->
        <activity android:name=".activity.CacheClearActivity" />
        <activity android:name=".activity.TestActivity" />

        <!--Android N 申请访问存储空间-->
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="ye.droid.jarvis.fileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/jarvis_provider_paths" />
        </provider>
        <!--监听手机重启-->
        <receiver android:name=".broadcast.receiver.RebootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!--短信监听服务-->
        <service android:name=".service.SmsListenerService" />
        <!--位置变化监听服务-->
        <service android:name=".service.LocationChangeService" />
    </application>

    <!--使用网络权限-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" /><!--接受手机重启权限-->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!--短信相关-->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <!--定位相关权限-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
</manifest>